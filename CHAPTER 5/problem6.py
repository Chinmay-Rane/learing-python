#make a dict for 4 friends where name as keys and fav language can be entered
d={}
name=input("Enter the name: ")
lang=input("Enter favourite language: ")

d.update({name:lang})

name=input("Enter the name: ")
lang=input("Enter favourite language: ")

d.update({name:lang})

name=input("Enter the name: ")
lang=input("Enter favourite language: ")

d.update({name:lang})

name=input("Enter the name: ")
lang=input("Enter favourite language: ")

d.update({name:lang})

print(d)

#problem 7 if two names are the same the dict will just get updated
#problem 8 if two langs are same the value will be assigned to both